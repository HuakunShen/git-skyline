{
  "name": "git-skyline",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "postinstall": "bun scripts/init-env.ts",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "turbo": "^2.0.7"
  },
  "packageManager": "pnpm@9.5.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
